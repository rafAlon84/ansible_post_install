---
# tasks file for Python
- name: Install Python3
  apt:
    name: python3
    state: present
    update_cache: yes

- name: Install Pip
  apt:
    name: python3-pip
    state: present
    update_cache: yes
